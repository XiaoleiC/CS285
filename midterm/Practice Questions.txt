Berkeley CS 185/285

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

CS 185/285 Midterm (Practice Questions)
Student ID:

Name:

Instructions
1. This is a list of practice questions provided to help with your studying. The style, length, and
difficulty of these problems may not necessarily reflect those of the actual midterm.
2. All questions are multiple-choice. Mark your answers in the separate answer sheet. On the
exam, we will grade solely based on your answer choices on the separate answer
sheet.

1

Berkeley CS 185/285

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

CS 185/285 Midterm (Practice Questions – Answer Sheet)
Student ID:

Name:

Only answers on the answer sheet will be graded. Fill in the bubble(s) for each problem. For
single-choice questions, fill exactly one bubble. For “select all” questions, fill all bubbles corresponding
to the correct letters. Bubbles must be filled completely and unambiguously. Your score will
be deducted if the course staff and/or OCR misinterpret your answer due to ambiguous marking.

#

Problem

Circle answer(s)

1.1

Distribution Shift for Success/Failure Tasks (1)

a

b

c

d

1.2

Distribution Shift for Success/Failure Tasks (2)

a

b

c

d

1.3

Distribution Shift for Success/Failure Tasks (3)

a

b

c

d

2.1

Open Loop Control (1)

a

b

c

d

2.2

Open Loop Control (2)

a

b

c

d

3

Entropy regularized policy gradient

a

b

c

d

4

Learning in Simulation

a

b

c

d

e

f

5.1

Computing the Policy Gradient (1)

a

b

c

d

e

f

5.2

Computing the Policy Gradient (2)

a

b

c

d

e

f

5.3

Computing the Policy Gradient (3)

a

b

c

d

e

f

g

6.1

Imitation learning analysis (1)

a

b

c

d

6.2

Imitation learning analysis (2)

a

b

c

d

7.1

IQL variants (1)

a

b

c

d

7.2

IQL variants (2)

a

b

c

d

8

LLM MDPs

a

b
j

c

d

e

f

g

k

l

m

i

e

f

e

f

e

g

h

9.1

Multi-Step Dynamics Models (1)

=

/

≤

/

≥

/

?

and

=

/

≤

/

≥

/

?

9.2

Multi-Step Dynamics Models (2)

=

/

≤

/

≥

/

?

and

=

/

≤

/

≥

/

?

10

Managing Distribution Shift in Offline RL

a

b

c

d

If you have any additional comments (e.g., additional assumptions or interpretations you made, notes to the
course staff, etc.), use the space below.

2

Berkeley CS 185/285

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Potentially Useful Formulas
Some potentially useful formulas are provided below. These formulas are provided purely for convenience and
may be ignored. They are not guaranteed to be directly applicable to individual problems, and you are fully
responsible for determining their validity in each context or adapting them accordingly. In case of any conflict,
you must follow the definitions and assumptions stated in each problem.
• Policy gradients
"

H
X

∇θ J(θ) = Eτ ∼pθ (τ )

!
∇θ log πθ (at | st )

t=1

∇θ J(θ) ≈ Eτ ∼pθ (τ )

"H
X

H
X

!#
r(st , at )

t=1




∇θ log πθ (at | st ) r(st , at ) + γ V̂ (st+1 ) − V̂ (st )
π

#

π

t=1

• Q-learning


2 
′
′
L(Q) = E(s,a,s′ )∼D Qθ (s, a) − [r(s, a) + γ max
Qθ̄ (s , a )]
′
a

• Performance difference formula for off-policy policy gradients
′

J(θ ) − J(θ) =

∞
X
t=1

γ

t−1




πθ′ (at | st ) πθ
A (st , at )
Est ∼pθ′ (st ) Eat ∼πθ (at |st )
πθ (at | st )

• KL and TV divergences
DKL (p(x)∥q(x)) = Ex∼p(x) [log p(x) − log q(x)]
1X
DTV (p(x), q(x)) =
|p(x) − q(x)|
2 x
• Variational inference
log p(x) ≥ Ez∼qϕ (z|x) [log pθ (x | z) + log p(z)] + H(qϕ (z | x)).
• Inverse RL
∇ψ L ≈

N
M
1 X
1 X
p(τ ) exp(rψ (τj ))
∇ψ rψ (τi ) − P
wj ∇ψ rψ (τj ), where wj =
N i=1
π(τj )
w
j
j
j=1

3

Berkeley CS 185/285

1

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Distribution Shift for Success/Failure Tasks

Imagine that instead of maximizing reward, we want to train policies that “succeed” according to a binary
success criterion. Success can be evaluated on the last time step of a trajectory τ = (s1 , a1 , . . . , sH , aH ):
sH ∈ Ω is successful for some set Ω. We will write pθ (τ successful) to denote the probability that the last
state in a trajectory sampled by running πθ (a|s) is contained in Ω.
πθ will be trained with supervised learning using a dataset D = {(si , ai )}N
i=1 generated by a deterministic
optimal Markovian policy π ⋆ (si ) that always succeeds, such that ai = π ⋆ (si ). Use ptrain (st ) to denote the
distribution of training states, and use pπθ (st ) to denote the distribution of states under the policy πθ at time
step t. Assume Est ∼ptrain (st ) [πθ (at ̸= π ⋆ (st )|st )] ≤ ϵ for all t.
We’ll derive an upper bound on pθ (τ failed), based on the behavioral cloning cost bound derivation.
(1) First, letting 1(sH ∈
/ Ω) be 1 if sH is not in Ω (0 otherwise), and 1(sH ∈ Ω) be 1 if sH is in Ω (0
otherwise), which of the following quantities is equal to the probability that the policy fails?
(a) pθ (τ failed) = ptrain (sH )1(sH ∈
/ Ω)
P
(b) pθ (τ failed) = sH ptrain (sH )1(sH ∈
/ Ω)
P
(c) pθ (τ failed) = sH ptrain (sH )1(sH ∈ Ω)
(d) pθ (τ failed) = pπθ (sH )1(sH ∈
/ Ω)
P
(e) pθ (τ failed) = sH pπθ (sH )1(sH ∈
/ Ω)
P
(f) pθ (τ failed) = sH pπθ (sH )1(sH ∈ Ω)
(2) If we follow similar logic as we saw in the behavioral
cloning error bound proof in lecture to derive an
P
upper bound on pθ (τ failed), using the fact that st |pπθ (st ) − ptrain (st )| ≤ ϵt as derived in lecture, which of
the following is a step in the derivation of the bound?

P
(a) pθ (τ failed) ≤ sH |pπθ (sH ) − ptrain (sH )| + ptrain (sH ) 1(sH ∈ Ω)

P
(b) pθ (τ failed) ≤ sH |pπθ (sH ) − ptrain (sH )| + pπθ (sH ) 1(sH ∈ Ω)

P
(c) pθ (τ failed) ≤ sH |pπθ (sH ) − ptrain (sH )| + ptrain (sH ) 1(sH ∈
/ Ω)

P
(d) pθ (τ failed) ≤ sH |pπθ (sH ) − ptrain (sH )| + pπθ (sH ) 1(sH ∈
/ Ω)
(3)

Based on the above, which of these is the best (i.e., tightest among others) upper bound on pθ (τ failed)?

(a) pθ (τ failed) ≤ ϵ
(b) pθ (τ failed) ≤ ϵH
(c) pθ (τ failed) ≤ ϵH 2
(d) pθ (τ failed) ≤ 1 − ϵ
(e) pθ (τ failed) ≤ 1 − ϵH
(f) pθ (τ failed) ≤ 1 − ϵH 2

4

Berkeley CS 185/285

2

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Open Loop Control

Consider an MDP (S, A, r, P, γ), where r : S → R and 0 < γ < 1. Recall that a sequence of actions At defines
an open loop policy. Each distribution π(a | s) conditioned on state defines a closed loop policy.
As usual, the discounted total return can be defined equivalently for both types of policies,

J(π) =

∞
X

γ t r(st )

and

J(A∗ ) =

t=0

∞
X

γ t r(st )

t=0

as well as the discounted state occupancy measure:
pπ (s) =

∞
X

γ t p(st = s)

and

pA (s) =

t=0

∞
X

γ t p(st = s)

t=0

(1) Which of the following is not true? Select one response.
(a) There may be no stationary (time-invariant) optimal open loop policies in an MDP.
(b) In an MDP with deterministic dynamics and initial state, the best open loop policy attains the same
performance as the best closed loop policy, i.e., J(π ∗ ) = J(A∗ )
(c) There exists an MDP with nonnegative, bounded rewards where the best closed-loop policy gets return
1 but the best open loop policy gets return 0, i.e., J(π ∗ ) = 1 and J(A∗ ) = 0
(d) Consider an open loop policy A. There always exists a deterministic, Markovian closed-loop policy π
that attains the same discounted (marginal) state occupancy, so pA = pπ
. Select one response. You may
(2) The cross entropy method when applied to open loop control
assume a max horizon of H with 1 < M < H with M elites selected at each iteration.
(a) With unlimited iterations, will almost surely (with probability arbitrarily close to 1) converge to an
optimal sequence of actions when the sampling distribution p(At ) is a multivariate Gaussian N (µ, Σ)
and sample mean µ and covariance Σ are computed from the previous iteration’s elites.
(b) Is the same as random shooting when the sampling distribution p(At ) is uniform over A
(c) Will only converge when the sampling distribution depends on the previous iteration’s elites.
(d) Doesn’t require gradients through the model
(e) Can only be used with discrete action spaces

5

Berkeley CS 185/285

3

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Entropy regularized policy gradient

Recall that H(πθ (a|s)) = −Ea∼πθ (a|s) [log πθ (a|s)]. Assuming that the action space is intractably large and
discrete (so no closed form expression for the entropy) and there is one time step (i.e., a bandit, such
that Q(s, a) = r(s, a)), derive a tractable unbiased policy gradient expression with samples for the entropyregularized objective of the form


J(θ) = Es∼p(s) Ea∼πθ (a|s) [r(s, a)] + H(πθ (a|s)) .
Use a constant baseline b.
PN
(a) ∇θ J(θ) ≈ N1 i=1 ∇θ log πθ (a|s)[r(s, a) + log πθ (a|s) − b]
PN
(b) ∇θ J(θ) ≈ N1 i=1 ∇θ log πθ (a|s)[r(s, a) − log πθ (a|s) − b]
PN
(c) ∇θ J(θ) ≈ N1 i=1 ∇θ log πθ (a|s)[r(s, a) − b]
PN
(d) ∇θ J(θ) ≈ N1 i=1 ∇θ log πθ (a|s)[− log πθ (a|s) − b]

4

Learning in Simulation

Suppose we have an MDP M = (S, A, r, P, γ) with 0 ≤ r(s) ≤ 1. We consider the problem of learning in
a model f (s′ | s, a) of the dynamics P (s′ | s, a). Learning with a model f can be viewed as learning in the
modified MDP F = (S, A, r, f, γ).
Denote by πT∗ the optimal policy in an MDP T . The regret of any policy π in T is the amount by which it is
suboptimal relative to πT∗ , stated as
R̃T (π) = J T (πT∗ ) − J T (π),
where
J T (π) =

∞
X

γ t r(st )

t=0

is the standard discounted return for an MDP T .
When we train in the simulator with model f of the dynamics, we are learning πF∗ . Our hope with a good
enough simulator is that it will transfer to the environment M , or in other words R̃M (πF∗ ) will be low.
Suppose DKL (P(s′ |s, a)∥f (s′ | s, a)) < ε2 for any s, a . Which of the following upper bounds on the regret
R̃M (πF∗ ) hold? Select all that apply.
Hint: Pinsker’s inequality gives
2δ(p, q) =

X

|p(x) − q(x)| ≤

p
2DKL (p∥q)

X

√
(a) R̃M (πF∗ ) ≤ εγ 2(1 − γ)−2
(b) R̃M (πF∗ ) ≤ εγ
(c) R̃M (πF∗ ) ≤ ε

p

p

(d) R̃M (πF∗ ) ≤ 2εγ(1 − γ)−1
p
(e) R̃M (πF∗ ) ≤ εγ 1/2(1 − γ)−3/2
√
(f) R̃M (πF∗ ) ≤ εγ 2(1 − γ)−1
√
(g) R̃M (πF∗ ) ≤ ε 2(1 − γ)−5/2

1/2(1 − γ)−2

1/2(1 − γ)−2

6

Berkeley CS 185/285

5

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Computing the Policy Gradient

Consider the following MDP:
a1 ,a2

a2

→

s0

a1 ,a2

←
a1

a1 ,a2

r=2/γ

←

→

→

←
s3

a2

→

r=1

← s2
→

←

a1

←

→

←
→

s1

s4

We assume that the initial state is s0 , and there are actions A = {a1 , a2 } that correspond to the edges marked
on the diagram. Assume the rewards are zero except for the edges annotated on the diagram.
We learn a policy πθ parameterized by θ ∈ [0, 1] such that at any state s ∈ {s0 , s1 , s2 , s3 , s4 }:
πθ (a1 | s) = 1 − θ
πθ (a2 | s) = θ.
We wish to optimize the discounted return:
∞
hX
i
J(θ) = E
γ t r(st , at ) .
t=0

(1) Compute the policy gradient ∇θ J(θ) for this MDP.
(a)

θ(1 − 2θ) − 1
1 − γ3

(d)

4θγ − 1
1 − γ3

(b)

2θ − 1
1 − γ3

(e)

θ(2θ − 1)
1 − γ2

(c)

1 + θ(γ − 2θ)
1 − γ2

(f)

2θγ − 1
1 − γ2

(2) What is the maximum achievable return J(θ∗ )? Assume γ > 1/2.
(a)

1
1−γ

(b)

2γ
1−γ

(c)

1
1 − γ2

2
1 − γ2
γ
(e)
1 − γ3

(d)

(f)

2γ
1 − γ3

(3) For which initial values of θ does the policy gradient recover the optimal policy when γ = 3/4? Assume
updates are performed with gradient descent θ ← θ + α∇J(θ) for a sufficiently small learning rate α.
(a) θ > 1/4

(e) θ < 1/3

(b) θ < 2/3
(f) θ > 3/4
(c) θ > 1/3
(d) θ < 1/2

(g) θ < 3/4

7

Berkeley CS 185/285

6

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Imitation learning analysis

Consider the problem of imitation learning within a discrete MDP with horizon T and an expert policy π ∗ .
We gather expert demonstrations from π ∗ and fit an imitation policy πθ to these trajectories so that
T

Epπ∗ (s) πθ (a ̸= π ∗ (s) | s) =

1X
Ep ∗ (s ) πθ (at ̸= π ∗ (st ) | st ) ≤ ε,
T t=1 π t

i.e., the expected likelihood that the learned policy πθ disagrees with the expert π ∗ within the training
distribution pπ∗ of states drawn from random expert trajectories is at most ε.
For convenience, the notation pπ (st ) indicates the state distribution under π at time step t while p(s) indicates
the state marginal of π across time steps, unless indicated otherwise.
Consider the expected return of the learned policy πθ for a state-dependent reward r(st ), where we assume
the reward is bounded with |r(st )| ≤ 1:
J(π) =

T
X

Epπ (st ) r(st ).

t=1

(1) Express the upper bound of J(π ∗ ) − J(πθ ) in terms of T and ε. It is enough to express the upper bounds
using big O notation (in T and ε).
(a) O(T 2 ε)
(b) O(T ε)
(c) O(T 3 ε)
(d) O(T 2 ε2 )
(2) If the reward only depends on the last state (i.e., r(st ) = 0 for all t < T ), how does the upper bound
change (or not)? Select the tightest new bound.
(a) O(T 2 ε)
(b) O(T ε)
(c) O(T 3 ε)
(d) O(T 2 ε2 )

8

Berkeley CS 185/285

7

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

IQL variants

Implicit Q-learning (IQL) is an offline RL algorithm that trains a Q-function Qϕ (s, a) and a value function
Vψ (s) using expectile regression. Consider the following four variants of IQL losses:
(a)

L(ϕ) = Es,a,s′ ∼D [(Qϕ (s, a) − r(s, a) − γVψ (s′ ))2 ]
L(ψ) = Es,a∼D [ℓτ2 (Vψ (s) − Qϕ̄ (s, a))]

(b)

L(ϕ) = Es,a,s′ ∼D [(Qϕ (s, a) − r(s, a) − γVψ (s′ ))2 ]
L(ψ) = Es,a∼D [ℓτ2 (Qϕ̄ (s, a) − Vψ (s))]

(c)
(d)

L(ϕ) = Es,a,s′ ,a′ ∼D [ℓτ2 (Qϕ (s, a) − r(s, a) − γQϕ̄ (s′ , a′ ))].
L(ϕ) = Es,a,s′ ,a′ ∼D [ℓτ2 (r(s, a) + γQϕ̄ (s′ , a′ ) − Qϕ (s, a))].

In the above losses, D denotes an offline dataset, (s, a, s′ , a′ ) denotes an adjacent (state, action, next state,
next action) pair sampled from the dataset, ℓτ2 (x) = |1(x > 0) − τ |x2 denotes the expectile loss with expectile
τ ∈ [0.5, 1), and Qϕ̄ (s, a) denotes the target Q-function.
(1) Among the four variants, which ones approximate the optimal Q-function in an unbiased manner when
τ → 1 (under the assumption that D has sufficient coverage)? Select all of the variants (a-d) that apply.
(Hint: Think about whether they are unbiased in stochastic environments.)
(2) Notice that when τ = 0.5, IQL learns the behavioral Q-function Qπβ (s, a), where πβ is the data-collecting
policy for the dataset D. Among the four variants, which ones approximate the behavioral Q-function in an
unbiased manner when τ = 0.5 (under the assumption that D has sufficient coverage)? Select all of the
variants (a-d) that apply.

9

Berkeley CS 185/285

8

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

LLM MDPs

Consider an LLM MDP MLLM = (S, A, T , r) defined as follows.
The state space
S = {(x1 , x2 , . . . , xn ) : xi ∈ W, 1 ≤ n ≤ H}
is the set of all possible token sequences, where W is the token vocabulary and H is the maximum sequence
length.
The action space is
A = W,
i.e., actions correspond to selecting the next token.
The transition dynamics T is deterministic concatenation: for a state s = (x1 , x2 , . . . , xn ) and an action a = w,
the next state is deterministically given by s′ = (x1 , x2 , . . . , xn , w). The initial state is always a fixed prompt.
For simplicity, assume that there is no end-of-sequence token, so all final token sequences have length exactly
H.
The reward r is 1 only when the current token sequence is of full length and correctly answers the task, and
0 otherwise.
For this LLM MDP MLLM , select all correct statements:
(a) Subtracting a baseline from a policy gradient estimator provides no benefit.
(b) Learning a dynamics model provides no benefit.
(c) Training a value function is impossible in practice.
(d) When we have a state-value function (V (s)), additionally training an action-value function that outputs
a scalar value (Q(s, a)) provides no benefit.
(e) There is no bias-variance tradeoff in policy gradient estimators.
(f) We do not need variational inference in the control-as-inference framework.
(g) DQN is incompatible with the LLM MDP.
(h) TRPO is incompatible with the LLM MDP.
(i) Offline RL is incompatible with the LLM MDP.
(j) Next token prediction is equivalent to behavioral cloning.
(k) Value iteration becomes equivalent to policy iteration.
(l) Policy iteration can always find the optimal policy in one policy update.
(m) Maximizing the entropy of the policy’s action distribution (H(π(a|s))) at each state s is always enough
to maximize the entropy of the final state distribution (H(pπ (sH ))).

10

Berkeley CS 185/285

9

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Multi-Step Dynamics Models

Let M = (S, A, T , r) be a finite-horizon MDP with horizon H. For each n ∈ {1, 2, 3}, define the length-n
action-chunked MDP M(n) = (S, An , T (n) , r(n) ) as follows.
The action space is the set of length-n action chunks,
An = {(a0 , . . . , an−1 ) : ai ∈ A}.
The transition dynamics T (n) is the marginalized n-step transition dynamics induced by M:
!
Z n−1
Y
(n)
T (st+n |st , (at , . . . , at+n−1 )) =
T (st+i+1 |st+i , at+i ) dst+1 dst+2 · · · dst+n−1 .
i=0

For simplicity, assume that H is divisible by all n ∈ {1, 2, 3}.
The reward function r(n) is the expected sum of rewards while executing the chunk:
!
! n−1
Z n−2
X
Y
(n)
r(st+i , at+i ) dst+1 dst+2 · · · dst+n−1 .
T (st+i+1 |st+i , at+i )
r (st , (at , . . . , at+n−1 )) =
i=0

i=0

Let J (n) (π(n) ) denote the expected return of an action-chunking policy π(n) ((at , at+1 , . . . , at+n−1 )|st ) in M(n) .
That is,
H

−1
n
X
(n)
(n)
r(n) (st , at ) ,
J (n) (π(n) ) = Eτ (n) ∼pπ (τ (n) ) 
(n)

t=0

where
τ (n) = ( s0 , (a0 , a1 , . . . , an−1 ), sn , (an , an+1 , . . . , a2n−1 ), . . . , sH−n , (aH−n , . . . , aH−1 ))
|{z} |
| {z } |
{z
} |{z} |
{z
}
{z
}
(n)

s0

(n)

(n)

(n)

s1

a0

a1

(n)

sH
n

−1

(n)

aH
n

−1

∗
is sampled from the action-chunking policy π(n) in the action-chunked MDP M(n) . Let π(n)
be an optimal
(n)
(n)
action-chunking policy that maximizes J
in M .

Fill in each box below by choosing the most suitable symbol from {=, ≤, ≥, ?}. Choose ? when none of the
∗
relations (=, ≤, ≥) always hold between the two terms. Do not assume that π(n)
is necessarily a deterministic
policy (recall that an optimal policy need not be unique, and some optimal policies may be stochastic when
multiple actions lead to the same value, even in deterministic environments).
(1) For any M,
∗
J (1) (π(1)
)

∗
J (2) (π(2)
)

∗
J (3) (π(3)
).

∗
J (2) (π(2)
)

∗
J (3) (π(3)
).

(2) If the dynamics T is deterministic,
∗
J (1) (π(1)
)

11

Berkeley CS 185/285

10

Deep Reinforcement Learning, Decision Making, and Control

Spring 2026

Managing Distribution Shift in Offline RL

In this question, we will analyze how reward bonuses can be used to manage distribution shift in offline RL.
We consider an MDP M = (S, A, r, p) with offline data D collected by a policy πβ . We denote by pπ the
distribution over states induced by π.
As a motivating example, consider the soft actor-critic (SAC) algorithm discussed in HW 3. When updating,
SAC adds an adjustment of b(s, a) = − log π(a | s) to the target values Eπ(a|s) [Q(s, a) + b(s, a)] to enforce
a maximum entropy regularizer on the policy. Alternatively, we could impose a similar form of entropy
regularization by adding the bonus directly to the reward. In expectation, we would optimize
1
J(π) − λ Es∼pπ Eπ(a|s) log π(a | s)
Es,a∼pπ [r(s, a) + λb(s, a)] = H
1
=H
J(π) + λ Es∼pπ H[π(a | s)].

Thus, entropy regularization can also be implemented by adding the bonus b(s, a) to rewards. In the following
parts, you will show how a similar reward bonus can be used to constrain distribution shift in offline RL.
We wish to learn a Q function and policy π from the offline data D under some constraint D(π, πβ ) ≤ ε with
the following update:
Q(s, a) ← r(s, a) + Ea′ ∼π [Q (s′ , a′ )]
where

(1)

π = arg max Es,a∼pπ [Q(s, a)] s.t. D(π, πβ ) ≤ ε.
π

(2)

Directly enforcing the constraint in (2) is challenging with the environment rewards r(s, a), so we will implicitly
enforce the constraint with a Lagrangian, modifying the reward to r̄(s, a) = r(s, a)+λb(s, a) in (1). The overall
optimization then becomes:
Q(s, a) ← r̄(s, a) + Ea′ ∼π [Q (s′ , a′ )]
where

π = arg max Es,a∼pπ [Q(s, a)].
π

You may assume that λ > 0 is selected appropriately to enforce the constraint as follows:

 

arg max Es,a∼pπ [Q(s, a)] − λD(π, πβ ) = arg max Es,a∼pπ [Q(s, a)] s.t. D(π, πβ ) ≤ ε .
π

π

You may also assume access to the distributions π(a | s) and πβ (a | s) in your answers.
Suppose we wish to learn π under the following KL-divergence constraint:
D(π, πβ ) = Es∼pπ DKL [π(a | s)∥πβ (a | s)].
How can we enforce this constraint by adding a bonus b(s, a) to the reward r̄(s, a) = r(s, a) + λb(s, a)?
(a) b(s, a) = − log π(a | s)
(b) b(s, a) = log πβ (a | s)
(c) b(s, a) = log π(a | s) − log πβ (a | s)
(d) b(s, a) = log πβ (a | s) − log π(a | s)

12

(3)
(4)

